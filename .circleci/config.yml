version: 2
jobs:
  build:
    docker:
      - image: circleci/android:api-28-alpha
    steps:
      - run:
          name: Build debug APK and release APK
          command: |
            ./gradlew clean
            ./gradlew check